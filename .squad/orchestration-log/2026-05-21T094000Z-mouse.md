# 2026-05-21T09:40:00Z — Mouse Structured I/O Refactor

## Agent Manifest Execution

- **Agent:** Mouse (claude-sonnet-4.6)
- **Background/Sync:** background
- **Commit:** f226d45
- **PR:** #20
- **Status:** Tests passing

## Work Summary

Completed structured agent I/O refactor for Sprint 1 Foundry workflow addressing three critical gaps:

1. **Multilanguage Support:** Intent Classifier now explicitly supports user messages in any language and propagates `language_hint` through the workflow.
2. **Structured Outputs:** SQL Builder and Intent Classifier calls now use `response_format` with strict Pydantic structured outputs (`IntentResult`, `SqlPlan`), eliminating brittle JSON-in-message parsing.
3. **Structured Inputs:** SQL Builder and Query Executor agent definitions now declare typed structured inputs (`intentResult`, `tableSchemas`, `buId`, `userQuestion`, `sqlPlan`, `executionResult`, `userLanguage`), with the runtime passing values through `structured_inputs` instead of prompt embedding.

## Impact

- Agent contracts are now explicit and typed at the SDK level.
- Workflow remains compatible with existing MCP tool execution and safe-failure handling.
- Prompts can focus on reasoning rather than JSON formatting rules.
- Multilingual support preserved end-to-end.

## Downstream Dependencies

- Tank: needs awareness for integration work
- Apoc: needs awareness for integration work
