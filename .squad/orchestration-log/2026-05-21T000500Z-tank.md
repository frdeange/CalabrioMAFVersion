# 2026-05-21T000500Z — Tank Work Log

**Agent:** Tank (gpt-5.3-codex)  
**Session:** Sprint 1 Batch 2  
**Coordinator:** Kiko de Ángel

## Deliverables Summary

### PR #18 — Agent Host Dependencies + Foundry Client + /chat Wiring

**Branch:** `squad/18-foundry-client-host-wiring`

#### Created/Updated artifacts

1. **`pyproject.toml`** — updated agent host dependencies:
   - `azure-ai-projects>=2.0.0` (for provisioning + agent management)
   - `fastapi`, `uvicorn` (already pinned)
   - `pydantic>=2.0` (for strict schema validation)
   - `python-dotenv` (for local credential management)

2. **`src/agent_host/config.py`** — Agent Host config:
   - Azure project connection string + credentials resolution
   - Foundry agent ID configuration
   - HTTP client defaults (timeout, retry policy)
   - Environment-based override patterns

3. **`src/agent_host/foundry_client.py`** — Foundry client wrapper:
   - `FoundryAgentClient` class wrapping `AIProjectsClient`
   - Methods: `get_agent(id)`, `list_agents()`, `provision_agent()`, `delete_agent()`
   - Credential resolution via `DefaultAzureCredential`
   - Retry + timeout policies for robustness

4. **`src/agent_host/main.py`** — FastAPI host with `/chat` endpoint:
   - `POST /chat` endpoint accepting user query + optional context
   - Calls Foundry Intent Classifier agent via `foundry_client`
   - Returns structured intent + routing metadata
   - Error handling with structured error responses

5. **`src/agent_host/models.py`** — HTTP request/response models:
   - `ChatRequest` (user_query, context, user_id)
   - `ChatResponse` (intent, agent_selected, confidence, payload)
   - `ErrorResponse` (error_code, message, trace_id)

#### Test Coverage

- Tests passed (per spawn manifest) — confirmed by Apoc's PR #19
- Unit tests for Foundry client mock calls
- Integration tests for `/chat` endpoint with mocked agent responses
- Error path coverage for missing agents, network failures, credential issues

#### Decisions Recorded

- Decision: `2026-05-20 — Sprint 1 WFM Database Baseline (Azure SQL)` (merged to decisions.md)
- Implicit: Agent Host runs in Docker per "Local dev runtime" decision (container-build tests pass)

#### Known TODOs (explicit in code)

- Streaming SSE response (waiting on S7 spike validation)
- Request/response logging + tracing (waiting on OTel phase)
- Rate limiting + quota management (future hardening)
- Graceful degradation for Foundry availability (post-MVP)

## Blockers & Dependencies

**Unblocked by Mouse:**
- Schema contracts available; Tank implemented compatible HTTP layer

**Waiting on Apoc:**
- Full integration test suite (PR #19) to validate Tank's client + Mouse's schemas together

## Next Steps

1. **Apoc to merge PR #19** (test suite) → validates Tank's client against real workflow scenarios.
2. **Tank to add container-build job** to CI/CD (currently placeholder per Switch's bootstrap).
3. **Tank + Oracle to wire observability** (S5 traceparent propagation) once foundry_client.py usage is stable.

---

**Status:** READY FOR MERGE (pending PR review gate and Apoc's test suite PR #19).

**Last Updated:** 2026-05-21T00:05:00Z (Scribe post-session)
