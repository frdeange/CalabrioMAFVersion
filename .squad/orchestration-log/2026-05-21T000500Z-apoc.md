# 2026-05-21T000500Z — Apoc Work Log

**Agent:** Apoc (gpt-5.3-codex)  
**Session:** Sprint 1 Batch 2  
**Coordinator:** Kiko de Ángel

## Deliverables Summary

### PR #19 — Integration + Unit Test Suite for Foundry Host + Workflow

**Branch:** `squad/19-test-suite-foundry-integration`

#### Created artifacts

1. **`tests/test_schemas.py`** — Pydantic schema validation:
   - Intent classification response validation (intent name, confidence, routing metadata)
   - SQL plan envelope validation (structured SQL, parameter bindings, execution hints)
   - Execution result formatting (structured results, error handling, response envelope)
   - Schema coercion tests (valid + invalid payloads, type mismatches, boundary conditions)

2. **`tests/test_foundry_client.py`** — Foundry client wrapper tests:
   - Mock `AIProjectsClient` behavior
   - `get_agent()` success + error paths
   - `list_agents()` pagination handling
   - `provision_agent()` with YAML prompt validation
   - `delete_agent()` cleanup verification
   - Credential resolution tests (DefaultAzureCredential mocking)
   - Timeout + retry policy coverage

3. **`tests/test_workflow.py`** — End-to-end workflow orchestration:
   - Intent → SQL Builder → Executor pipeline sequencing
   - Conditional routing logic (metadata caching, shortlist logic)
   - Error propagation (metadata failures, SQL validation failures, execution failures)
   - Catalog caching validation (listTables cache hit, per-session lifecycle)
   - Edge cases: empty metadata, no matching tables, SQL injection attempts

4. **`tests/test_chat_endpoint.py`** — FastAPI `/chat` endpoint:
   - POST request success path (user query → Intent Classifier → response)
   - Request validation (malformed JSON, missing required fields)
   - Error responses (500 Internal Server Error, 422 Unprocessable Entity)
   - Header propagation (x-user-context, x-request-id for tracing)
   - Status code correctness

5. **`tests/conftest.py`** — Pytest fixtures:
   - Mocked Foundry agents (intent-classifier, sql-builder, query-executor)
   - Sample metadata catalogs (BU-DEMO schema, tables, views)
   - FastAPI test client
   - Temporary Azure credential mocking

6. **`tests/test_query_kpis.py`** — Query validation pack integration:
   - 22 NL queries (20 safe + 2 adversarial) from Apoc's decision pack
   - Token count measurement (prompt tokens, completion tokens)
   - Latency measurement (Intent → SQL → Executor pipeline)
   - Accuracy scoring (expected vs actual result envelope)
   - KPI targets validation (token budget, latency SLA, accuracy threshold)
   - BU-filter compliance check (query scoping to CWFM-DEMO)

#### Test Coverage

- **105 tests passed** (per spawn manifest)
- **2 skipped** (waiting on Azure resources provisioned in Fase 0b — S5 traceparent, S8 Foundry connectivity)
- Unit coverage: schemas, client, config, error handling
- Integration coverage: workflow orchestration, pipeline sequencing, caching behavior
- Endpoint coverage: request/response validation, error codes, headers
- Adversarial coverage: 2 SQL injection attempts, malformed queries, auth failures

#### Decisions Recorded

- Decision: `2026-05-20 — Sprint 1 Query Validation Pack (Apoc)` (merged to decisions.md)

#### Known TODOs (explicit in code)

- Live Foundry agent calls (skipped; requires Fase 0b provisioning)
- S5 traceparent propagation end-to-end (skipped; requires APIM real provisioning)
- Prompt Shields / content filtering tests (waiting on Foundry SDK feature bump)
- Performance profiling under load (future performance optimization phase)
- Adversarial query corpus expansion (post-MVP — currently 2 queries)

## Blockers & Dependencies

**Unblocked by:**
- Mouse's schemas + Tank's Foundry client (both completed PR #20 + PR #18)

**Waiting on:**
- Fase 0b provisioning (APIM, AOAI, Foundry sandbox) to enable skipped live tests

## Next Steps

1. **All PRs (#18, #19, #20) to merge** → Sprint 1 Batch 2 complete.
2. **Apoc to expand adversarial corpus** (Phase 2 quality gate).
3. **Apoc + Oracle to wire Content Safety / Prompt Shields** (once Foundry SDK supports).

---

**Status:** READY FOR MERGE (all 105 tests pass; 2 skipped by design awaiting Fase 0b resources).

**Last Updated:** 2026-05-21T00:05:00Z (Scribe post-session)
