# 2026-05-21T000500Z — Mouse Work Log

**Agent:** Mouse (claude-sonnet-4.6)  
**Session:** Sprint 1 Batch 2  
**Coordinator:** Kiko de Ángel

## Deliverables Summary

### PR #20 — Foundry Agent Provisioning + MAF Workflow Skeleton

**Branch:** `squad/20-foundry-provisioning`

#### Created artifacts

1. **`scripts/provision_agents.py`** — provisions three persisted Foundry agents (`wfm-intent-classifier`, `wfm-sql-builder`, `wfm-query-executor`) from YAML definitions using `azure-ai-projects>=2.0.0`.

2. **Foundry prompt YAMLs** under `src/agent_host/prompts/`:
   - `intent-classifier.yaml` — intent routing prompt
   - `sql-builder.yaml` — SQL planning prompt
   - `query-executor.yaml` — execution/result formatting prompt

3. **`src/agent_host/app/schemas.py`** — strict Pydantic v2 schemas for:
   - Intent classification response
   - SQL plan envelope
   - Execution result formatting
   - Top-level workflow response

4. **`src/agent_host/app/workflow.py`** — pre-MAF workflow skeleton:
   - Foundry agent orchestration via `project.get_openai_client()`
   - Single-per-session catalog caching
   - Intent → SQL Builder → Executor conditional pipeline
   - Error handling for metadata, SQL, execution failures
   - TODO hooks for middleware, HMAC headers, Prompt Shields, SQL pre-validation, OTel

#### Test Coverage

- 31 tests passed (per spawn manifest)
- Unit tests for schema validation
- Integration tests for workflow orchestration
- Mocked Foundry responses for deterministic testing

#### Decisions Recorded

- Decision: `2026-05-20 — Sprint 1 Foundry Agent Provisioning + Workflow Skeleton` (merged to decisions.md)
- Decision: `2026-05-20 — MAF Workflow Design: Dynamic Metadata + Three Specialized Agents` (merged to decisions.md)

#### Known TODOs (explicit in code)

- FunctionMiddleware wiring (waiting on MAF `WorkflowBuilder` + `MCPStreamableHTTPTool`)
- HMAC header generation/validation (blocked by MCP tool callable signature)
- Prompt Shields / PII detection (waiting on Foundry SDK version bump)
- SQL pre-validation and injection checks (future hardening phase)
- OpenTelemetry SDK integration (deferred to phase with observability gate)

## Blockers & Dependencies

**Waiting on Tank:**
- Updated `pyproject.toml` with Foundry 2.0+ SDK (required for provisioning script)
- `foundry_client.py` wrapper (required for Agent Host agent management)
- Updated `main.py` wiring `/chat` endpoint

**Waiting on Apoc:**
- Integration tests with live Foundry agent calls (requires Tank's infrastructure changes first)

## Next Steps

1. **Tank to merge PR #18** (Foundry client + host wiring) → unblocks agent integration.
2. **Apoc to create PR #19** (test suite using Mouse's schemas + Tank's client).
3. **Mouse to wire native MAF `WorkflowBuilder`** once both upstream PRs land (Phase 1 Batch 3).

---

**Status:** READY FOR MERGE (pending PR review gate and Tank's PR #18 merge).

**Last Updated:** 2026-05-21T00:05:00Z (Scribe post-session)
